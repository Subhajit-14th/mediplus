import 'package:flutter/material.dart';

class BillingOperatorRootScreen extends StatelessWidget {
  const BillingOperatorRootScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Billing Operator',
        ),
      ),
    );
  }
}
