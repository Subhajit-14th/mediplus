import 'package:flutter/material.dart';

class PackagerRootScreen extends StatelessWidget {
  const PackagerRootScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Packager',
        ),
      ),
    );
  }
}
