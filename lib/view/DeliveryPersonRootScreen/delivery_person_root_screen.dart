import 'package:flutter/material.dart';

class DeliveryPersonRootScreen extends StatelessWidget {
  const DeliveryPersonRootScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Delivery Person',
        ),
      ),
    );
  }
}
